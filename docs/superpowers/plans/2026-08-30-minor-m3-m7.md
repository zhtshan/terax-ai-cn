# M-3~M-7 五项 Minor 修复实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 修复终端 tab 存量审查遗留的五项 Minor（M-3 subtitleFor 双写、M-4 blocks split 静默拒绝、M-5 planSpaceRemoval as 强转、M-6 private tab 通知泄漏、M-7 boot 前 newTab 无 cold 标记）。

**Architecture:** 五项彼此独立，每项一个小提交。M-3 提取纯函数消除双写；M-4 在命令面板补禁用理由；M-5 类型层重构；M-6 在通知桥补 private 守卫；M-7 四个 tab 创建函数补既有 `cold` 标记，变热走既有 choke point。均为既有模式的对齐，不引入新机制。

**Tech Stack:** React 19 + TypeScript（严格模式）、vitest + @testing-library/react（happy-dom）、i18next（zh-CN/en，setup 全局初始化）、pnpm、Biome。

**Spec:** `docs/superpowers/specs/2026-08-30-minor-m3-m7-design.md`

## Global Constraints

- 导入一律 `@/…` 路径别名，禁止跨模块相对路径（同目录内 `./xxx` 不受限）。
- TypeScript 严格模式，无 `any`。
- 注释默认无；必要时只写 1-2 行 WHY，不写 WHAT。
- 无 em-dash、无 emoji（代码/注释/commits 都不用）。
- commit message 用中文，并以 `Co-Authored-By: Claude <noreply@anthropic.com>` 结尾。
- 测试基建：`src/test-setup.ts` 已全局初始化 i18n（默认 zh-CN）并 stub 了 `@tauri-apps/api/event`；测试直接用 `vitest` 的 `describe/expect/it` 全局或显式导入皆可（现有文件均显式导入）。
- i18n 断言用 `i18next.t("key")` 比对而非硬编码文案，语言切换不脆。
- 交付前全量清单：`pnpm lint && pnpm check-types && pnpm test && cd src-tauri && cargo clippy --all-targets --locked -- -D warnings && cargo nextest run --locked`。

---

### Task 1: M-3 subtitleFor 提取到 tabLabel 消除双写

**Files:**
- Modify: `src/modules/tabs/lib/tabLabel.ts`（文件末尾追加 `subtitleFor`）
- Modify: `src/modules/tabs/index.ts:7`（导出 `subtitleFor`）
- Modify: `src/modules/tabs/TabSwitcherHud.tsx:1-16`（删本地实现，改导入）
- Modify: `src/modules/spaces/SpaceSwitcher.tsx:9`（导入处）与 `:62-73`（删本地实现）
- Test: `src/modules/tabs/lib/tabLabel.test.ts`（追加用例）

**Interfaces:**
- Consumes: `Tab` 联合类型（`src/modules/tabs/lib/useTabs.ts`），`labelFor` 同文件既有模式。
- Produces: `export function subtitleFor(t: Tab): string | null`，从 `@/modules/tabs` 公开导出，供 TabSwitcherHud 与 SpaceSwitcher 使用。签名与返回值语义和两处被删的本地实现逐字节一致。

- [ ] **Step 1: 在 tabLabel.test.ts 追加失败测试**

在文件末尾追加（文件顶部导入行改为 `import { labelFor, subtitleFor } from "./tabLabel";`，类型导入改为 `import type { EditorTab, PreviewTab, TerminalTab } from "./useTabs";`）：

```ts
describe("subtitleFor", () => {
  it("returns the last two cwd segments for terminal tabs", () => {
    expect(
      subtitleFor(terminalTab({ cwd: "/Users/me/projects/terax-ai" })),
    ).toBe("projects/terax-ai");
  });

  it("returns null for terminal tabs without cwd", () => {
    expect(subtitleFor(terminalTab())).toBeNull();
  });

  it("handles Windows-style cwd separators", () => {
    expect(subtitleFor(terminalTab({ cwd: "C:\\Users\\me\\proj" }))).toBe(
      "me/proj",
    );
  });

  it("returns the parent directory for editor tabs", () => {
    const editor: EditorTab = {
      id: 3,
      kind: "editor",
      spaceId: "default",
      title: "a.ts",
      path: "/Users/me/proj/src/a.ts",
      dirty: false,
      preview: false,
    };
    expect(subtitleFor(editor)).toBe("src");
  });

  it("returns the parent directory for markdown tabs", () => {
    const markdown: MarkdownTab = {
      id: 5,
      kind: "markdown",
      spaceId: "default",
      title: "notes.md",
      path: "/Users/me/proj/docs/notes.md",
    };
    expect(subtitleFor(markdown)).toBe("docs");
  });

  it("returns null for tab kinds without a path", () => {
    const preview: PreviewTab = {
      id: 4,
      kind: "preview",
      spaceId: "default",
      title: "localhost",
      url: "http://localhost:5173",
    };
    expect(subtitleFor(preview)).toBeNull();
  });
});
```

注意：导入行相应加 `MarkdownTab`（`import type { EditorTab, MarkdownTab, PreviewTab, TerminalTab } from "./useTabs";`）。

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test src/modules/tabs/lib/tabLabel.test.ts`
Expected: FAIL，报 `subtitleFor` 未从 `./tabLabel` 导出（或导入错误）。

- [ ] **Step 3: 在 tabLabel.ts 末尾实现**

```ts
/**
 * Secondary hint shown next to a label in switcher surfaces (HUD rows, space
 * overview). One shared copy so TabSwitcherHud and SpaceSwitcher stay in
 * lockstep.
 */
export function subtitleFor(t: Tab): string | null {
  if (t.kind === "terminal") {
    if (!t.cwd) return null;
    const segs = t.cwd.split(/[\\/]/).filter(Boolean);
    return segs.slice(-2).join("/") || t.cwd;
  }
  if (t.kind === "editor" || t.kind === "markdown") {
    const segs = t.path.split(/[\\/]/).filter(Boolean);
    return segs.slice(-2, -1)[0] ?? null;
  }
  return null;
}
```

- [ ] **Step 4: 切换两个调用方**

`src/modules/tabs/index.ts:7` 改为：

```ts
export { labelFor, subtitleFor } from "./lib/tabLabel";
```

`src/modules/tabs/TabSwitcherHud.tsx`：删除第 8-16 行的本地 `subtitleFor` 函数，第 3 行改为：

```ts
import { labelFor, subtitleFor } from "./lib/tabLabel";
```

`src/modules/spaces/SpaceSwitcher.tsx`：删除第 62-73 行的本地 `subtitleFor` 函数，第 9 行改为：

```ts
import { labelFor, subtitleFor, type Tab, TabIcon } from "@/modules/tabs";
```

- [ ] **Step 5: 运行测试与类型检查**

Run: `pnpm test src/modules/tabs/lib/tabLabel.test.ts && pnpm check-types`
Expected: 全部 PASS，无类型错误。

- [ ] **Step 6: Commit**

```bash
git add src/modules/tabs/lib/tabLabel.ts src/modules/tabs/lib/tabLabel.test.ts src/modules/tabs/index.ts src/modules/tabs/TabSwitcherHud.tsx src/modules/spaces/SpaceSwitcher.tsx
git commit -m "refactor(tabs): subtitleFor 提取到 tabLabel 消除双写

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 2: M-4 blocks 标签页 split 命令补禁用理由

**Files:**
- Modify: `src/modules/command-palette/commands.ts:78-82`（`splitDisabled` 增 blocks 分支）
- Modify: `src/i18n/locales/en.json:637`（`commandPalette.disabled` 增 key）
- Modify: `src/i18n/locales/zh.json:632`（同上）
- Test: Create `src/modules/command-palette/commands.test.ts`

**Interfaces:**
- Consumes: `createCommandItems(ctx: CommandPaletteActionContext): PaletteItem`（commands.ts:68），`PaletteItem.disabledReason?: string`（types.ts:15），`MAX_PANES_PER_TAB` 与 `Tab`（`@/modules/tabs`）。
- Produces: 无新接口。行为变化：active tab 为 blocks terminal 时，`pane.splitRight`/`pane.splitDown` 两项的 `disabledReason === i18next.t("commandPalette.disabled.blocksTab")`。

- [ ] **Step 1: 写失败测试**

创建 `src/modules/command-palette/commands.test.ts`：

```ts
import i18next from "i18next";
import { describe, expect, it } from "vitest";
import type { Tab } from "@/modules/tabs";
import {
  createCommandItems,
  type CommandPaletteActionContext,
} from "./commands";

function ctxWithTabs(
  tabs: Tab[],
  activeId: number = tabs[0]?.id ?? 0,
): CommandPaletteActionContext {
  return {
    tabs,
    activeId,
    searchTarget: null,
    explorerRoot: null,
    home: null,
    openNewTab: () => {},
    openNewBlock: () => {},
    openNewPrivate: () => {},
    openNewEditor: () => {},
    openNewPreview: () => {},
    openGitGraph: () => {},
    toggleSourceControl: () => {},
    closeActiveTabOrPane: () => {},
    splitPaneRight: () => {},
    splitPaneDown: () => {},
    focusSearch: () => {},
    focusExplorerSearch: () => {},
    toggleSidebar: () => {},
    toggleAi: () => {},
    askAiSelection: () => {},
    openSettings: () => {},
    openKeyboardShortcuts: () => {},
    spaces: [],
    activeSpaceId: null,
    openSpacesOverview: () => {},
    newSpace: () => {},
    switchSpace: () => {},
  };
}

function terminalTab(
  over: Partial<Extract<Tab, { kind: "terminal" }>> = {},
): Tab {
  return {
    id: 1,
    kind: "terminal",
    spaceId: "default",
    title: "shell",
    paneTree: { kind: "leaf", id: 2 },
    activeLeafId: 2,
    ...over,
  };
}

const SPLIT_IDS = ["pane.splitRight", "pane.splitDown"];

describe("createCommandItems split disable reasons", () => {
  it("disables both split commands on a blocks tab", () => {
    const items = createCommandItems(ctxWithTabs([terminalTab({ blocks: true })]));
    for (const id of SPLIT_IDS) {
      expect(items.find((i) => i.id === id)?.disabledReason).toBe(
        i18next.t("commandPalette.disabled.blocksTab"),
      );
    }
  });

  it("keeps split enabled on a regular terminal tab", () => {
    const items = createCommandItems(ctxWithTabs([terminalTab()]));
    for (const id of SPLIT_IDS) {
      expect(items.find((i) => i.id === id)?.disabledReason).toBeUndefined();
    }
  });

  it("disables split with the no-terminal reason on non-terminal tabs", () => {
    const editor: Tab = {
      id: 2,
      kind: "editor",
      spaceId: "default",
      title: "a.ts",
      path: "/tmp/a.ts",
      dirty: false,
      preview: false,
    };
    const items = createCommandItems(ctxWithTabs([editor]));
    for (const id of SPLIT_IDS) {
      expect(items.find((i) => i.id === id)?.disabledReason).toBe(
        i18next.t("commandPalette.disabled.noTerminalTab"),
      );
    }
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test src/modules/command-palette/commands.test.ts`
Expected: 第一个用例 FAIL（blocks tab 的 disabledReason 现为 undefined），其余两个 PASS。

- [ ] **Step 3: 加 i18n key**

`src/i18n/locales/en.json` 的 `commandPalette.disabled` 块，在 `"noTerminalTab"` 行前插入：

```json
      "blocksTab": "Blocks tab",
```

`src/i18n/locales/zh.json` 同位置插入：

```json
      "blocksTab": "Blocks 标签页",
```

- [ ] **Step 4: 改 splitDisabled**

`src/modules/command-palette/commands.ts:78-82` 改为：

```ts
  const splitDisabled = !activeTerminalTab
    ? i18next.t("commandPalette.disabled.noTerminalTab")
    : activeTerminalTab.blocks
      ? i18next.t("commandPalette.disabled.blocksTab")
      : activePaneCount >= MAX_PANES_PER_TAB
        ? i18next.t("commandPalette.disabled.paneLimit")
        : undefined;
```

- [ ] **Step 5: 运行测试与类型检查**

Run: `pnpm test src/modules/command-palette/commands.test.ts && pnpm check-types`
Expected: 全部 PASS。

- [ ] **Step 6: Commit**

```bash
git add src/modules/command-palette/commands.ts src/modules/command-palette/commands.test.ts src/i18n/locales/en.json src/i18n/locales/zh.json
git commit -m "fix(command-palette): blocks 标签页 split 命令显示禁用理由

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 3: M-5 planSpaceRemoval 用类型谓词替换 as 强转

**Files:**
- Modify: `src/modules/tabs/lib/useTabs.ts:234-236`
- Test: `src/modules/tabs/lib/planSpaceRemoval.test.ts`（追加混合 tab 类型用例）

**Interfaces:**
- Consumes: `planSpaceRemoval`（useTabs.ts:224，签名不变）、`TerminalTab`、`leafIds`。
- Produces: 无接口变化。`disposeLeafIds` 仍为删除空间内全部 terminal 叶 id 数组。

- [ ] **Step 1: 追加失败测试**

`planSpaceRemoval.test.ts` 文件顶部导入改为：

```ts
import { describe, expect, it } from "vitest";
import { planSpaceRemoval, type EditorTab, type Tab, type TerminalTab } from "./useTabs";
```

在 `describe("planSpaceRemoval", ...)` 末尾追加用例：

```ts
  it("disposes only terminal leaves when the space mixes tab kinds", () => {
    const editor: EditorTab = {
      id: 2,
      kind: "editor",
      spaceId: "a",
      title: "a.ts",
      path: "/tmp/a.ts",
      dirty: false,
      preview: false,
    };
    const tabs: Tab[] = [term(1, "a"), editor, term(3, "b")];
    const plan = planSpaceRemoval(tabs, 1, "a", "b", undefined, counterFrom(100));
    expect(plan?.disposeLeafIds).toEqual([10]);
    expect(plan?.tabs.map((t) => t.id)).toEqual([2, 3]);
  });
```

- [ ] **Step 2: 运行测试确认通过**

Run: `pnpm test src/modules/tabs/lib/planSpaceRemoval.test.ts`
Expected: PASS。注意：本任务是类型层重构，现实现行为正确，该用例此步预期就通过；它的价值是锁定谓词改写后的行为不变。若 FAIL 则现状与认知不符，停下按 systematic-debugging 排查。

- [ ] **Step 3: 改写 disposeLeafIds**

`useTabs.ts:234-236` 改为：

```ts
  const disposeLeafIds = removed
    .filter((t): t is TerminalTab => t.kind === "terminal")
    .flatMap((t) => leafIds(t.paneTree));
```

- [ ] **Step 4: 运行测试与类型检查**

Run: `pnpm test src/modules/tabs/lib/planSpaceRemoval.test.ts && pnpm check-types`
Expected: 全部 PASS，无 `as TerminalTab` 残留于该函数。

- [ ] **Step 5: Commit**

```bash
git add src/modules/tabs/lib/useTabs.ts src/modules/tabs/lib/planSpaceRemoval.test.ts
git commit -m "refactor(tabs): planSpaceRemoval 用类型谓词替换 as 强转

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 4: M-6 private 终端抑制 agent 通知

**Files:**
- Create: `src/modules/agents/lib/privateTab.ts`
- Modify: `src/modules/agents/components/AgentNotificationsBridge.tsx:36-63`（`route()` 加守卫）
- Test: Create `src/modules/agents/lib/privateTab.test.ts`

**Interfaces:**
- Consumes: `Tab`（`@/modules/tabs`，`TerminalTab.private?: boolean`）；`AgentNotificationsBridge.tsx` 的 `route(session, kind, ctx, t)`，`ctx.tabs: Tab[]`、`session.tabId: number`。
- Produces: `export function isPrivateTab(tabs: Tab[], tabId: number): boolean`。行为约定：private terminal tab 的 attention/finished 一律不路由通知；`store.setStatus` 照旧（内部状态保留，显示侧本就抑制）。

- [ ] **Step 1: 写失败测试**

创建 `src/modules/agents/lib/privateTab.test.ts`：

```ts
import { describe, expect, it } from "vitest";
import type { TerminalTab } from "@/modules/tabs";
import { isPrivateTab } from "./privateTab";

function terminal(over: Partial<TerminalTab> = {}): TerminalTab {
  return {
    id: 1,
    kind: "terminal",
    spaceId: "default",
    title: "shell",
    paneTree: { kind: "leaf", id: 2 },
    activeLeafId: 2,
    ...over,
  };
}

describe("isPrivateTab", () => {
  it("matches private terminal tabs", () => {
    expect(isPrivateTab([terminal({ private: true })], 1)).toBe(true);
  });

  it("does not match regular terminal tabs", () => {
    expect(isPrivateTab([terminal()], 1)).toBe(false);
  });

  it("does not match missing tabs or non-terminal kinds", () => {
    const editor: Tab = {
      id: 2,
      kind: "editor",
      spaceId: "default",
      title: "a.ts",
      path: "/tmp/a.ts",
      dirty: false,
      preview: false,
    };
    expect(isPrivateTab([editor], 2)).toBe(false);
    expect(isPrivateTab([], 1)).toBe(false);
  });
});
```

顶部需另加 `import type { Tab } from "@/modules/tabs";`（与 TerminalTab 合并为一行多导入亦可）。

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test src/modules/agents/lib/privateTab.test.ts`
Expected: FAIL，模块 `./privateTab` 不存在。

- [ ] **Step 3: 实现 privateTab.ts**

```ts
import type { Tab } from "@/modules/tabs";

/** Private terminals hide agent activity on the tab icon; notifications must
 * stay silent for the same reason (lock screens, screen shares). */
export function isPrivateTab(tabs: Tab[], tabId: number): boolean {
  const t = tabs.find((x) => x.id === tabId);
  return t?.kind === "terminal" && t.private === true;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `pnpm test src/modules/agents/lib/privateTab.test.ts`
Expected: PASS。

- [ ] **Step 5: 接入通知桥**

`src/modules/agents/components/AgentNotificationsBridge.tsx`：

导入区（第 7 行 `import { displayAgent } from "../lib/format";` 附近）加：

```ts
import { isPrivateTab } from "../lib/privateTab";
```

`route()` 函数体（第 41-63 行）改为（仅新增第一行守卫，其余与现状一致）：

```ts
function route(
  session: AgentSession,
  kind: "attention" | "finished",
  ctx: Ctx,
  t: T,
): void {
  if (isPrivateTab(ctx.tabs, session.tabId)) return;
  const info = tabInfo(ctx.tabs, session.leafId);
  const name = displayAgent(session.agent);
  const heading =
    kind === "attention"
      ? t("agents.notifications.needsInput", { name })
      : t("agents.notifications.finished", { name });

  routeAgentNotification({
    source: "terminal",
    agent: session.agent,
    kind,
    title: heading,
    body: info?.title,
    // Stop fires every turn, so finished only updates the bell; attention toasts.
    allowToast: kind === "attention",
    tabId: session.tabId,
    leafId: session.leafId,
    onActivate: () => ctx.onActivate(session.tabId, session.leafId),
  });
}
```

`handleSignal` 中 `store.setStatus` / `maybeTriggerManagedReview` 一律不动：private 抑制的是外发通知，不是内部状态跟踪。

- [ ] **Step 6: 回归测试**

Run: `pnpm test src/modules/agents/ && pnpm check-types`
Expected: 全部 PASS。

- [ ] **Step 7: Commit**

```bash
git add src/modules/agents/lib/privateTab.ts src/modules/agents/lib/privateTab.test.ts src/modules/agents/components/AgentNotificationsBridge.tsx
git commit -m "fix(agents): private 终端抑制 agent 通知对齐图标抑制

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 5: M-7 boot 前 newTab 系列补 cold 标记

**Files:**
- Modify: `src/modules/tabs/lib/useTabs.ts:432-449`（newTab）、`:451-469`（newBlockTab）、`:478-495`（newAgentTab）、`:497-514`（newPrivateTab）
- Test: Create `src/modules/tabs/lib/useTabs.cold.test.tsx`

**Interfaces:**
- Consumes: `useTabs()` 返回的 `newTab/newBlockTab/newAgentTab/newPrivateTab/markBooted`；既有变热 choke point（useTabs.ts:311-318，`booted` 门控）。
- Produces: 无签名变化。行为约定：这四个函数创建的 tab 带 `cold: true`，boot 前不挂载不 spawn PTY；boot 后激活即由 choke point 变热。签名与返回值不变。

- [ ] **Step 1: 写失败测试**

创建 `src/modules/tabs/lib/useTabs.cold.test.tsx`：

```tsx
/// <reference types="vitest/globals" />
import { act, renderHook } from "@testing-library/react";
import { describe, expect, it } from "vitest";
import type { useTabs } from "./useTabs";

type Hooks = ReturnType<typeof useTabs>;

const creators: [string, (h: Hooks) => number][] = [
  ["newTab", (h) => h.newTab()],
  ["newBlockTab", (h) => h.newBlockTab()],
  ["newAgentTab", (h) => h.newAgentTab("/tmp", "agent").tabId],
  ["newPrivateTab", (h) => h.newPrivateTab()],
];

describe("useTabs new-tab creators mark cold", () => {
  it.each(creators)("%s creates the tab cold before boot", (_name, create) => {
    const { result } = renderHook(() => useTabs());
    let id = -1;
    act(() => {
      id = create(result.current);
    });
    expect(result.current.tabs.find((t) => t.id === id)?.cold).toBe(true);
  });

  it("warms the active new tab once booted", () => {
    const { result } = renderHook(() => useTabs());
    let id = -1;
    act(() => {
      id = result.current.newTab();
    });
    act(() => {
      result.current.markBooted();
    });
    expect(result.current.tabs.find((t) => t.id === id)?.cold).toBe(false);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `pnpm test src/modules/tabs/lib/useTabs.cold.test.tsx`
Expected: 四个 creator 用例 FAIL（`cold` 为 undefined），warm 用例 PASS（现状无 cold 可言，markBooted 后当然非 cold，此用例守护修复后的变热路径）。

- [ ] **Step 3: 四个创建函数补 cold: true**

在 `newTab`、`newBlockTab`、`newAgentTab`、`newPrivateTab` 的对象字面量中，`spaceId: activeSpaceIdRef.current,` 行后各插入一行（与 `newTabInSpace` 的字段顺序一致）：

```ts
        cold: true,
```

- [ ] **Step 4: 运行测试与相关回归**

Run: `pnpm test src/modules/tabs/lib/ && pnpm test src/modules/spaces/`
Expected: 全部 PASS。若出现依赖「newTab 后立即非 cold」的既有用例失败，先按 systematic-debugging 定位：该行为正是本次要移除的 boot 前泄漏，测试应改断言而非回退实现；若是真实功能依赖（如 boot 前必须有可用 PTY），停下报告，不得硬改。

- [ ] **Step 5: Commit**

```bash
git add src/modules/tabs/lib/useTabs.ts src/modules/tabs/lib/useTabs.cold.test.tsx
git commit -m "fix(tabs): boot 前 newTab 系列补 cold 标记

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

### Task 6: 全量验证（CI 同步清单）

**Files:** 无新改动；仅验证。

- [ ] **Step 1: 跑完整清单**

Run:
```bash
pnpm lint && pnpm check-types && pnpm test && cd src-tauri && cargo clippy --all-targets --locked -- -D warnings && cargo nextest run --locked
```
Expected: 全绿。任何失败按 systematic-debugging 处理后重跑。

- [ ] **Step 2: 汇报**

汇报五项各自提交哈希与验证结果。本计划直接在 dev 分支小步提交，无分支合并步骤；不主动 push（dev 领先 origin 属既有状态，是否推送由用户决定）。
