# Tasks

- [x] 在 `sessionManager.ts` 中动态计算 TS SDK 路径并注入到 LSP 客户端
- [x] 重命名 `basename` 函数参数避免与 `path` 模块冲突
- [x] 运行 `pnpm check-types` 确认类型检查通过
- [x] 运行 `pnpm lint` 确认代码规范通过
- [x] 运行 `pnpm test` 确认测试通过
- [x] 运行 Rust clippy 和测试确认后端无问题
